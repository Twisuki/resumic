import { avatar } from "@server/repo/avatar"
import { health } from "@server/repo/health"
import { resume } from "@server/repo/resume"
import { user } from "@server/repo/user"

export const repo = { avatar, health, resume, user }
